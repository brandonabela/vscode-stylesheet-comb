
name: Main CI
on: [push, pull_request]

jobs:
  install:
    name: 📦 Install Dependencies
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Dependencies
        run: npm install

  test:
    name: 🧪 Unit Test
    runs-on: windows-latest
    needs: [install]
    steps:
      - name: Run Unit Tests
        run: npm run test

  lint:
    name: ✅ Lint
    runs-on: windows-latest
    needs: [install]
    steps:
      - name: Run Linter
        run: npm run lint

  build:
    name: 🛠 Build
    runs-on: windows-latest
    needs: [test, lint]
    steps:
      - name: Build Project
        run: npm build
